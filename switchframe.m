function switchframe(hObject, Eventdata, Frame1, Frame2)

% This function switches from one frame to another when both have already
% been created.

set(Frame1, 'Visible', 'off')
set(Frame2, 'Visible', 'on')

end